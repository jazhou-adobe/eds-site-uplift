/**
 * loads and decorates the notice info-bar
 * The block is styled entirely via CSS; no DOM transformation is required.
 * @param {Element} block The notice block element
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // no-op: presentation only
}
